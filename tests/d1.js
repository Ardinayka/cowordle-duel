import fs from 'node:fs';import {DatabaseSync} from 'node:sqlite';
export function testEnv(){const sql=new DatabaseSync(':memory:');for(const file of fs.readdirSync('drizzle').filter(x=>x.endsWith('.sql')).sort())sql.exec(fs.readFileSync('drizzle/'+file,'utf8'));const DB={withSession(){return this;},prepare(text){let args=[];return {bind(...v){args=v;return this;},async first(){return sql.prepare(text).get(...args);},async all(){return {results:sql.prepare(text).all(...args)};},_run(){return {meta:{changes:Number(sql.prepare(text).run(...args).changes)}};},async run(){return this._run();}};},async batch(statements){sql.exec('BEGIN');try{const result=statements.map(x=>x._run());sql.exec('COMMIT');return result;}catch(e){sql.exec('ROLLBACK');throw e;}}};return {sql,DB};}
